/// Settings Feature.
///
/// Provides the app-wide preferences (language & theme mode) along with
/// the main Settings page that hosts Language, Theme, and Reminders
/// sections. Choices are persisted via SharedPreferences.
library;

export 'presentation/presentation.dart';